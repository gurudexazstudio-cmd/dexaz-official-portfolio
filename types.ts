export interface ContactInfo {
  phone: string;
  email: string;
  address?: string;
}

export interface Founder {
  name: string;
  role: string;
  image: string;
}

export interface NavItem {
  label: string;
  href: string;
}